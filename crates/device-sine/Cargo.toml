[package]
name = "device-sine"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib"]

[dependencies]
engine-env = {path = "../engine-env"}
dsp = {path = "../dsp"}

[dev-dependencies]
value = {path = "../value"}
