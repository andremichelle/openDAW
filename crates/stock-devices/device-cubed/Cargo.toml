[package]
name = "device-cubed"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
libm = "0.2"
abi = {path = "../../abi"}
dsp = {path = "../../dsp"}
